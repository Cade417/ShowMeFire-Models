# Model Training Quickstart

This guide's content has moved to the [Model Lifecycle guide](model_lifecycle.md), which covers the same day-to-day workflow (sync data → retrain `fuel_moisture` → publish/import/promote → check in on the shadow-only models) alongside every other model family, in one canonical place.

- "Get fresh data" → Model Lifecycle guide, §1 ("Getting fresh data").
- "Retrain the fuel-moisture model" → Model Lifecycle guide, "Archetype A".
- "Publish and promote" → Model Lifecycle guide, §4–§5.
- "Check on the Fire Weather Risk (GLM) model" → Model Lifecycle guide, §6 ("Checking in on an already-shadow-scoring model").

Further reading unaffected by this move: [`retraining_and_fire_weather_risk_plan.md`](retraining_and_fire_weather_risk_plan.md) (design rationale), [`spatial_fuel_moisture_runbook.md`](spatial_fuel_moisture_runbook.md) (full RTMA/spatial operator detail).
